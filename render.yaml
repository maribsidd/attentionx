services:
  - type: web
    name: attentionx
    runtime: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # set manually in Render dashboard
    healthCheckPath: /_stcore/health
